export class Ap{
    numero: number;
    bloco: string;
    andar: number;
    morador: number;

    constructor(numero: number, bloco: string, andar: number, morador: number){
        this.numero = numero;
        this.bloco = bloco;
        this.andar = andar;
        this.morador = morador;
    }
}